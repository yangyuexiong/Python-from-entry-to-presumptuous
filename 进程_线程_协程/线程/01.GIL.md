### GIL

```text
线程:
操作系统切换和调度的最小单元

GIL(global interpreter lock)(全局解释锁)

Python中的一个线程对应于C语言中的一个线程

GIL使得同一时刻只有一个线程在一个CPU上执行字节码,无法将多个线程映射到多个CPU上执行(保证了线程安全,但性能不高,并发受限,无法运用多核优势)

GIL释放:
    1.GIL会根据内部策略对执行的字节码行数以及时间片释放
    2.遇到IO操作时主动释放
```
